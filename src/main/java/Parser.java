import java.util.Scanner;

public class Parser {
    Scanner scanner;

    public Parser() {
        scanner = new Scanner(System.in);
    }

    public void parse() {
        String input = scanner.nextLine();

        if(input.charAt(0) = 'D')
    }
}
